# BeastPhone AOSP Build Kit — version pins
# Android version update: change these values, then re-sync + rebuild.
# See docs/VERSION-UPDATE.md

AOSP_TAG="android-14.0.0_r67"
LUNCH_GSI="aosp_x86_64-ap2a-userdebug"
LUNCH_PHASE2="sdk_phone64_x86_64-ap2a-userdebug"
BUILD_ID="AP2A.240905.003"
KERNEL_PREBUILT_REL="kernel/prebuilts/6.6/x86_64/kernel-6.6"

# Parallelism (override: export BUILD_JOBS=16)
BUILD_JOBS="${BUILD_JOBS:-28}"

# Optional overrides (usually leave default)
# AOSP_DIR="$HOME/aosp"
# EXPORT_DIR="$HOME/beastphone-export"
# BEASTPHONE_WIN_USER="Anshul"
